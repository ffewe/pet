{
  "name": "desktop-pet-pixel-preview",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 pixel preview app for the desktop pet project.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "check": "node --check src/main.js && node --check src/preload.js && node --check src/pet-renderer.js && node --check src/sidebar-renderer.js && node --check src/main-renderer.js && node --check src/preview-tools.js && node --check src/renderer.js"
  },
  "devDependencies": {
    "electron": "^31.7.7"
  }
}
